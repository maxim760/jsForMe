2 примера, короче стили надо прописыавать не в самом стайле после первых скобоко типа просто в js коде типо того можно и класснэймом проворачивать , типа ? "name" : ""

<span
  style={
    text.length >= 0.9 * maxLen
      ? { fontSize: 20, fontWeight: 700 }
      : {}
  }
>

style={text.length === maxLen ? { color: "red" } : {}}

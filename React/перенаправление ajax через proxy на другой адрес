  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "npx json-server --watch public/db.json --port=3003"
  },
  "proxy": "http://localhost:3003", // прокси сюда перенаправит
const res = await axios.get(`/pizzas?${typeof category === "number" && `category=${category}`}&_sort=${typeSort}&_order=${order}`)
// proxy перенаправит на 3003

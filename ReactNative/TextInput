<TextInput
          value={value} 
          onChangeText={ text => changeHandler(text)} // если нужен только текст, лучше юзать onChangeText
          style={styles.input} 
          placeholder="Введите название дела"
          autoCorrect={false} // изначально автокоррекция - тру
          autoFocus={true}
          autoCapitalize="words" // при каждом новом слове по умолчанию клавиатура переводится на верхний регистр 
          keyboardType="phone-pad"
      />
keyboardType -  параметр для выбора типа показываемой клавиатуры

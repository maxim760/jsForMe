    const img = new Image();
    img.onload = function () {
       const width = this.width
       const height = this.height

    };
    img.src = activeImg.url;
    
    // это в какой нибудь метод он ченч он клик и тд

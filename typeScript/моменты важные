let fullName = "Bob Bobbington";
let age = 37;
// typescript сам понимает что 37 это число а "..." - строка ,не обязательно указывать
// так как передается значение напрямую а не через какие либо переменные, если через переменны то уже не понятено и надо прописывать тип 100%
// это назвается - интерференция - тайп скрипт сам понимает переданный тип,если он указан явно

// если в контракте функции прописано, что она возвращает воид или в интерфейсе который имплементриуется в классе написано что возвращает воид
// то это значин всё-равно что возвращать (но лучше чем эни, так как эни позволит обраащтся к несуществующим полям и тд)
// то есть сама функция может быть и намбер и стринг и воид в принципе тоже
